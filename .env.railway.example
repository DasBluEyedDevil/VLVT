# Railway Environment Variables Template
# Copy these to Railway Dashboard → Service → Variables

# ============================================================================
# AUTH SERVICE
# ============================================================================
PORT=3001
JWT_SECRET=GENERATE_WITH_openssl_rand_base64_64
DATABASE_URL=${{Postgres.DATABASE_URL}}
NODE_ENV=production
CORS_ORIGIN=*

# Optional for Google OAuth verification
# GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# ============================================================================
# PROFILE SERVICE
# ============================================================================
PORT=3002
DATABASE_URL=${{Postgres.DATABASE_URL}}
NODE_ENV=production
CORS_ORIGIN=*

# ============================================================================
# CHAT SERVICE
# ============================================================================
PORT=3003
DATABASE_URL=${{Postgres.DATABASE_URL}}
NODE_ENV=production
CORS_ORIGIN=*

# ============================================================================
# NOTES
# ============================================================================
# 1. DATABASE_URL uses Railway's service reference syntax
#    ${{Postgres.DATABASE_URL}} automatically connects to your Railway Postgres
#
# 2. Generate JWT_SECRET with: openssl rand -base64 64
#
# 3. CORS_ORIGIN:
#    - Use "*" for testing
#    - Use specific domains for production: "https://yourdomain.com"
#
# 4. NODE_ENV should be "production" to disable test login endpoint
#
# 5. Each service needs to be deployed separately in Railway
#    with its own set of these variables
