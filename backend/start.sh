#!/bin/sh
# This is a placeholder - the backend/ directory should not be deployed directly.
# Use auth-service/, profile-service/, or chat-service/ subdirectories instead.
echo "Backend root directory - no service to run."
echo "Run a service from backend/auth-service, backend/profile-service, or backend/chat-service."
exit 1
