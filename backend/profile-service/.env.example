PORT=3002
NODE_ENV=development
DATABASE_URL=postgresql://postgres:CHANGE_PASSWORD@postgres:5432/vlvt

# JWT Secret (MUST match auth-service)
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_RANDOM_SECRET_IN_PRODUCTION

# CORS Configuration
# Set to your frontend URL in production (e.g., https://yourdomain.com)
# For local development, use http://localhost:19006 or your Expo dev server URL
CORS_ORIGIN=http://localhost:19006

# =============================================================================
# Cloudflare R2 Storage Configuration (REQUIRED for image uploads)
# =============================================================================
# Get these from: Cloudflare Dashboard -> R2 -> Manage R2 API Tokens -> Create API Token
# Account ID is in: Cloudflare Dashboard -> R2 (shown in the URL or sidebar)
R2_ACCOUNT_ID=your_cloudflare_account_id
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_BUCKET_NAME=vlvt-images

# Presigned URL expiration time in seconds (default: 3600 = 1 hour)
# Shorter = more secure, longer = better caching
R2_URL_EXPIRY_SECONDS=3600

# =============================================================================
# AWS Rekognition Configuration (REQUIRED for selfie verification)
# =============================================================================
# Get from AWS Console > IAM > Users > Create Access Key
# Ensure the IAM user has AmazonRekognitionFullAccess permission
# AWS_ACCESS_KEY_ID=your_aws_access_key_id
# AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
# AWS_REGION=us-east-1

# =============================================================================
# Firebase Cloud Messaging (REQUIRED for push notifications)
# =============================================================================
# Download service account JSON from Firebase Console > Project Settings > Service Accounts
# Set GOOGLE_APPLICATION_CREDENTIALS to the path of the JSON file
# Or copy the credentials inline to FIREBASE_SERVICE_ACCOUNT (JSON string)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/firebase-service-account.json

# =============================================================================
# Monitoring & Error Tracking
# =============================================================================

# Sentry Error Tracking (RECOMMENDED for production)
# Get your DSN from: https://sentry.io/ -> Settings -> Projects -> [Your Project] -> Client Keys (DSN)
# Leave empty to disable Sentry error tracking for local development
SENTRY_DSN=
